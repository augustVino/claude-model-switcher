{
  "name": "claude-model-switcher",
  "version": "1.0.0",
  "type": "module",
  "description": "Switch AI providers in Claude Code CLI via @provider syntax",
  "bin": {
    "ccs": "./bin/ccs.ts"
  },
  "files": [
    "bin/",
    "src/"
  ],
  "license": "MIT",
  "engines": {
    "bun": ">=1.0"
  },
  "scripts": {
    "test": "bun test",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
