{
  "name": "@vinoorg/claude-model-switcher",
  "version": "1.1.0",
  "type": "module",
  "description": "Switch AI providers in Claude Code CLI via @provider syntax",
  "bin": {
    "ccs": "./bin/ccs.ts"
  },
  "files": [
    "bin/",
    "src/",
    "CHANGELOG.md"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/augustVino/claude-model-switcher.git"
  },
  "homepage": "https://github.com/augustVino/claude-model-switcher#readme",
  "bugs": {
    "url": "https://github.com/augustVino/claude-model-switcher/issues"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "scripts": {
    "test": "bun test",
    "check": "tsc --noEmit",
    "pub": "npm publish --access public",
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major",
    "release:patch": "standard-version --release-as patch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "standard-version": "^9.5.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "string-width": "^8.2.1"
  }
}
